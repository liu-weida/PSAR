package utils.enums;

public enum OperationStatus {
    // Définir différents résultats d’opérations
    SUCCESS, INSERT_ERROR, DATA_NOT_EXISTS, COMMAND_ERROR, DATA_EXISTS, EXCEPTION,
    LOCKED,ERROR, UNWRITTEN
}
